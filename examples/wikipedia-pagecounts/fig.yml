#mongodb:
#  image: mongo:2.6
#  net: bridge
#cassandra:
#  image: spotify/cassandra
#  net: bridge
elasticsearch:
  image: stratio/elasticsearch
  net: bridge
  environment:
    ES_CORS_ENABLED: "true"
  volumes:
    - /tmp/docker-ingestion-demo/elasticsearch:/data
kibana:
  image: stratio/kibana
  net: bridge
  links:
    - elasticsearch:elasticsearch
ingestiondemo:
  build: .
  #image: stratio/ingestion-wikipedia-demo:latest
  net: bridge
  links:
    #- mongodb:mongodb
    #- cassandra:cassandra
    - elasticsearch:elasticsearch
    - kibana:kibana
  volumes:
    - /tmp/docker-ingestion-demo/ingestiondemo/data:/demo/data
    - /tmp/docker-ingestion-demo/ingestiondemo/logs:/demo/logs
