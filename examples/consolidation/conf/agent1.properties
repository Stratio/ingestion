# The configuration file needs to define the sources,
# the channels and the sinks.
# Sources, channels and sinks are defined per agent,
# in this case called 'agent'
agent1.sources = seqGenSrc
agent1.channels = memoryChannel
agent1.sinks = avro-forward-sink

# For each one of the sources, the type is defined
agent1.sources.seqGenSrc.type = seq
 
# The channel can be defined as follows.
agent1.sources.seqGenSrc.channels = memoryChannel

# Each sink's type must be defined
agent1.sinks.avro-forward-sink.type = avro
agent1.sinks.avro-forward-sink.hostname = 127.0.0.1
agent1.sinks.avro-forward-sink.port = 60000

#Specify the channel the sink should use
agent1.sinks.avro-forward-sink.channel = memoryChannel

# Each channel's type is defined.
agent1.channels.memoryChannel.type = memory

# Other config values specific to each type of channel(sink or source)
# can be defined as well
# In this case, it specifies the capacity of the memory channel
agent1.channels.memoryChannel.capacity = 1000000
agent1.channels.memoryChannel.transactionCapacity = 10000